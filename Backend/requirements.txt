fastapi==0.139.0
# starlette fijado: fastapi solo pide >=0.46 y pip llega a jalar la serie 1.x,
# que rompe include_router en silencio. Se queda en la 0.46.x compatible.
starlette==0.46.2
uvicorn[standard]==0.51.0
sqlmodel==0.0.39
psycopg[binary]==3.3.4
pydantic-settings==2.14.2
# TestClient de FastAPI lo necesita para pruebas.
httpx==0.28.1
# Auth: hash de contraseñas, tokens JWT, y lectura del form de login.
bcrypt==5.0.0
pyjwt==2.13.0
python-multipart==0.0.32
# EmailStr de Pydantic lo necesita para validar formato de email.
email-validator==2.3.0
# ML: análisis de datos (pandas) + modelo Random Forest y métricas (scikit-learn).
pandas==3.0.3
scikit-learn==1.9.0
numpy==2.5.1
