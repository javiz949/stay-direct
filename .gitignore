__pycache__/
*.pyc
.venv/
venv/
node_modules/
.env
.DS_Store
dist/
build/
# Datos crudos del ML (grandes, reproducibles desde Inside Airbnb — no se versionan)
MLService/data/*.csv
MLService/data/*.csv.gz
# Checkpoints de Jupyter
.ipynb_checkpoints/
# Notebooks de experimentos personales (locales, no se versionan)
MLService/sandbox/
# Imágenes de apoyo local (no se versionan)
MLService/images/
# Documentación personal del autor (no se versiona)
docs/
